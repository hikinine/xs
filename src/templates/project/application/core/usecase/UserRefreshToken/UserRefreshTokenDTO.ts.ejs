import { BaseAuthenticationPayload } from './../../../base/abstract/Authentication';
import { IsString, Length, IsObject, IsDefined } from "class-validator"

export type UserRefreshTokenDTO = ValidatorUserRefreshTokenDTO

export class ValidatorUserRefreshTokenDTO  {
  @IsString()
  @Length(4, 100)
  refreshToken: string;
  
  @IsString()
  @Length(6, 100)
  accessToken: string
}

